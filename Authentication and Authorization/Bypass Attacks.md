`- **An attacker finds a way to skip or avoid a security control instead of breaking it directly.**
  
# **example**

- `A building has a **main entrance** with a security guard.

- `Instead of fighting the guard, the thief enters through an **unlocked back door**.
  
  ```[The thief didn't defeat security—they **bypassed** it.`]